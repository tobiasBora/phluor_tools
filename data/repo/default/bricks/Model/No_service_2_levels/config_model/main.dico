# You can here configure the application
# /!\ DON'T FORGET TO MODIFY THE FILE ./module.xml AND
# ../package/replacement.qdico TO REGISTER ANY NEW CONFIGURATION
# Configure the verbosity of the brick (-1 = default)
VERBOSE=-1


EXAMPLE=%%_EXAMPLE_%%
# It's often better to configure this file with the replacement.qdico (see line above)
# but you can put default settings here without putting them in the qdico file like that
SILENT_CONF=Configuration set by default
